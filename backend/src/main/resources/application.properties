# Application Properties for Library Management System
spring.application.name=librarymanagementsystem

# Server Port
server.port=8080

# ==============================
# Database Configuration (MySQL)
# ==============================

#spring.datasource.url=jdbc:mysql://localhost:3306/librarymanagementdb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=2002

## MySQL Driver
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================
# Database Configuration (PostgreSQL)
# ===================================

spring.datasource.url=jdbc:postgresql://db.fpzzjtxaftrejgcyzkaw.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=9AsANzMMxTLCAjta

# PostgreSQL Driver
spring.datasource.driver-class-name=org.postgresql.Driver

# =============================
# JPA / Hibernate Configuration
# =============================

# Automatically create/update tables based on Entity classes
spring.jpa.hibernate.ddl-auto=update

# Show SQL queries in console
spring.jpa.show-sql=true

# Format SQL output
spring.jpa.properties.hibernate.format_sql=true

## Hibernate dialect (MySQL ACTIVE)
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# PostgreSQL dialect (Use this when switching to PostgreSQL)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# =====================================================
# Validation Messages
# =====================================================

spring.messages.basename=messages